{
  "name": "vgraffiti",
  "version": "0.0.1",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wxt",
    "dev:firefox": "wxt -b firefox",
    "build": "wxt build",
    "zip": "wxt zip",
    "zip:dist": "npm run build && node scripts/package-extension.mjs",
    "postinstall": "wxt prepare",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "perfect-freehand": "^1.2.2"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.280",
    "typescript": "~5.7.3",
    "wxt": "^0.20.0"
  }
}
